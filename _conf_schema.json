{
  "number": {
    "type": "int",
    "title": "显示上限",
    "description": "批量开箱时展示结果的上限数量（默认 10）",
    "default": 10
  },
  "max_open_per_request": {
    "type": "int",
    "title": "单次开箱上限",
    "description": "每次命令最多允许开箱数量（默认 50）",
    "default": 50
  },
  "max_open_per_day": {
    "type": "int",
    "title": "每日开箱上限",
    "description": "每位用户每天最多可开箱数量，0 表示不限制（默认 500）",
    "default": 500
  },
  "daily_reset_time": {
    "type": "string",
    "title": "每日重置时间",
    "description": "按系统本地时间重置每日开箱额度，格式 HH:MM（默认 04:00）",
    "default": "04:00"
  },
  "api_host": {
    "type": "string",
    "title": "API 域名",
    "description": "API 请求域名（无需包含 https://）",
    "default": "api.csqaq.com"
  },
  "api_token": {
    "type": "string",
    "title": "API Token",
    "description": "API 认证 Token",
    "default": ""
  },
  "admins": {
    "type": "string",
    "title": "管理员QQ",
    "description": "多个 QQ 请用英文逗号分隔",
    "default": ""
  }
}
